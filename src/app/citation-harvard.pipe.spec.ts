import { CitationHarvardPipe } from './citation-harvard.pipe';

describe('CitationHarvardPipe', () => {
  it('create an instance', () => {
    const pipe = new CitationHarvardPipe();
    expect(pipe).toBeTruthy();
  });
});
